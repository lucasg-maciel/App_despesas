import 'package:flutter/material.dart';

class TelaResumoMensal extends StatelessWidget {
  const TelaResumoMensal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Resumo Mensal'),
      ),
      body: Center(
      ),
    );
  }
}
